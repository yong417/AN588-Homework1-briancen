
I will list my 5 struggles in the following when completing the task:

# 1. String Manipulation and Regular Expressions:
I struggled with using regular expressions in functions like gsub and str_split to correctly remove punctuation and split the string. Took me a few attempts
# 2. Matrix Indexing and Slicing:
Extracting specific rows and columns from the matrix was challenging, as I initially found it confusing to remember the correct syntax for subsetting.
# 3. Multi-Dimensional Array Creation and Indexing:
Constructing a 4-dimensional array and then correctly accessing its elements was a hurdle. Understanding how the dimensions are ordered and how to reference the correct indices required extra attention and practice with multi-dimensional data structures.
# 4. Overwriting Matrix Elements:
Overwriting parts of a matrix with new values was tricky, especially when the replacement values didn’t match the dimensions of the target slice.
# 5. Working with Lists and Data Frames:
Combining different data types (like vectors, matrices, and logical values) into a list and then navigating through nested elements was initially overwhelming. But at the end I got more used to them.
